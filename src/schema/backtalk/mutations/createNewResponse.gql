mutation createResponse($responseInput: responses_insert_input!) {
  insert_responses_one(object: $responseInput, on_conflict:{constraint: responses_pkey, update_columns:[response_content]}) {
    question_id
    response_content
  }
}
